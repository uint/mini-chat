import 'package:flutter/material.dart';

class MiniChatAppBar extends StatelessWidget {
  const MiniChatAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(title: const Text("hi"));
  }
}
